"use strict" //treate all JS code as a newer version
// alert("hello") we are using node js not browser it will only supported by browser only 
let isloggedin = true
null
let state
//null = standlone value
// symbols = used for uniqueness or when we are working on react
console.log(typeof "Bharat");
console.log(typeof null);
console.log(typeof isloggedin);
console.log(typeof state);